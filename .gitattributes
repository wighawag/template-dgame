# Force LF line endings for all text files  
* text=auto eol=lf

# Explicitly mark binary files to avoid conversion (e.g., images, executables)  
*.png binary  
*.jpg binary  
*.jpeg binary  
*.gif binary  
*.exe binary  
*.dll binary  
*.zip binary  

# LFS: all png files 
*.png filter=lfs diff=lfs merge=lfs -text